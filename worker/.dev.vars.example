# 本地 wrangler dev 时的环境变量样例。
# 复制为 .dev.vars 后按需修改；.dev.vars 不要提交到仓库。
# 生产环境的 vars 走 wrangler.toml 的 [vars] 或 `wrangler secret put`。

# CORS 白名单（逗号分隔），覆盖 wrangler.toml 里的默认值
CORS_ORIGINS="https://ustiniankw.github.io,http://localhost:8787"
